/*
* Procedure: getValue
* Obtiene el valor de un servicio configurable
* buscando por la llave devuelve el valor
*/
CREATE PROCEDURE getValue (IN key CHARACTER , IN element character)
RETURNS CHARACTER 
LANGUAGE JAVA 
EXTERNAL NAME "core.util.ConfigurableServiceMultiton.getValue";